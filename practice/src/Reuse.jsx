import React from 'react'

const Reuse = ({name,usn,phone}) => {
  return (
    <div>
        <h4>{name}</h4>
        <h4>{usn}</h4>
        <h4>{phone}</h4>

      
    </div>
  )
}

export default Reuse
